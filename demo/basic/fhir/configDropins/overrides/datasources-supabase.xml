<server>
    <!-- ============================================================== -->
    <!-- Clinivault → Supabase PostgreSQL datasource config             -->
    <!-- TENANT: default; DSID: default; TYPE: read-write               -->
    <!-- Uses IPv6 direct connection: db.*.supabase.co:5432             -->
    <!-- ============================================================== -->
    <dataSource id="fhirDatasourceDefaultDefault" jndiName="jdbc/fhir_default_default" type="javax.sql.XADataSource" statementCacheSize="200" syncQueryTimeoutWithTransactionTimeout="true">
        <jdbcDriver javax.sql.XADataSource="org.postgresql.xa.PGXADataSource" libraryRef="sharedLibPostgres"/>
        <properties.postgresql
             serverName="db.sjuqqlwmpidhhipplprn.supabase.co"
             portNumber="5432"
             databaseName="postgres"
             user="postgres"
             password="nlzATKDn2pFkfhLp"
             currentSchema="fhirdata"
             ssl="true"
             sslMode="require"
         />
        <connectionManager maxPoolSize="20" minPoolSize="2" connectionTimeout="60s" maxIdleTime="2m" numConnectionsPerThreadLocal="0"/>
    </dataSource>

    <!-- Bulk data / batch datasource — same Supabase DB -->
    <dataSource id="fhirbatchDS" jndiName="jdbc/fhirbatchDB" type="javax.sql.XADataSource" statementCacheSize="200" syncQueryTimeoutWithTransactionTimeout="true">
        <jdbcDriver javax.sql.XADataSource="org.postgresql.xa.PGXADataSource" libraryRef="sharedLibPostgres"/>
        <properties.postgresql
            serverName="db.sjuqqlwmpidhhipplprn.supabase.co"
            portNumber="5432"
            databaseName="postgres"
            user="postgres"
            password="nlzATKDn2pFkfhLp"
            currentSchema="fhirdata"
            ssl="true"
            sslMode="require"
          />
        <connectionManager maxPoolSize="10" minPoolSize="1" connectionTimeout="60s" maxIdleTime="2m"/>
    </dataSource>
</server>
