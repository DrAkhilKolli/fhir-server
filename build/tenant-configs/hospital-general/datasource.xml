<server>
    <!-- ============================================================== -->
    <!-- TENANT: hospital-general; DSID: default; TYPE: read-write      -->
    <!-- Supabase project: <hospital_supabase_project_ref>              -->
    <!--                                                                 -->
    <!-- Environment variables required at runtime:                     -->
    <!--   SUPABASE_HOSPITAL_GENERAL_DB_USER                            -->
    <!--   SUPABASE_HOSPITAL_GENERAL_DB_PASSWORD                        -->
    <!--                                                                 -->
    <!-- Upload to R2:                                                   -->
    <!--   fhir-tenant-configs/hospital-general/datasource.xml          -->
    <!-- ============================================================== -->
    <dataSource id="fhirHospitalGeneralDefault"
                jndiName="jdbc/fhir_hospital-general_default"
                type="javax.sql.XADataSource"
                statementCacheSize="200"
                syncQueryTimeoutWithTransactionTimeout="true"
                validationTimeout="30s">
        <jdbcDriver javax.sql.XADataSource="org.postgresql.xa.PGXADataSource"
                    libraryRef="sharedLibPostgres"/>
        <properties.postgresql
             serverName="db.<hospital_supabase_project_ref>.supabase.co"
             portNumber="5432"
             databaseName="postgres"
             user="${env.SUPABASE_HOSPITAL_GENERAL_DB_USER}"
             password="${env.SUPABASE_HOSPITAL_GENERAL_DB_PASSWORD}"
             currentSchema="fhirdata"
             ssl="true"
             sslmode="require"/>
        <connectionManager
             maxPoolSize="50"
             minPoolSize="5"
             connectionTimeout="60s"
             maxIdleTime="5m"
             numConnectionsPerThreadLocal="0"/>
    </dataSource>
</server>
